# ============================================================================
# 개발 모드용 Dockerfile
# 소스 코드를 마운트하여 실시간 변경사항 반영
# ============================================================================
FROM gradle:8.11.1-jdk21

# 작업 디렉토리 설정
WORKDIR /app

# Gradle 래퍼 실행 권한 부여 (이미 있을 수 있지만 확인)
RUN chmod +x ./gradlew || true

# 개발 모드에서는 소스 코드가 볼륨으로 마운트되므로
# 컨테이너 시작 시 Gradle로 애플리케이션 실행
# Spring Boot DevTools가 자동으로 변경사항 감지 및 재시작
CMD ["./gradlew", "bootRun", "--no-daemon", "--continuous"]



